pipeline {
   agent any

   stages {
      stage('Venv') {
        steps {
		python --version
        }
      }
      stage('Go') {
        steps{      
         sh 'robot Tests/tests.robot'
        }
		}
  }
} 