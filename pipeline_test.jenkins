pipeline {
   agent any

   stages {
      stage('Env') {
        steps{      
         withPythonEnv('python3') {
			sh 'pip install -r requirements.txt'
			sh 'robot Tests/tests.robot'
		}
        }
		}
  }
} 